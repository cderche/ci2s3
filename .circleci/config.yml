version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15.0
    steps:
      - checkout
      - run: ls
      # - run:
      #     name: Install angular cli
      #     command: sudo npm install -g @angular/cli
      # - run:
      #     name: Version of angular
      #     command: ng version
